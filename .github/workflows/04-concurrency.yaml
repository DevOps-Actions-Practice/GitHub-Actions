name: Concurrency
on: [push]
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true
jobs:
    concurrency_job:
        runs-on: ubuntu-latest
        steps:
        - name: step1
          run: sleep 20
        - name: step2
          run: sleep 20
        - name: step3
          run: sleep 20
        - name: step3
          run: sleep 20
        - name: step4
          run: sleep 20

        